# Copie para `.env` na raiz do app.
# URL do servidor (pasta `server/`). Obrigatório — o app não funciona sem API.
# No celular físico, use o IP da máquina na rede local (não use "localhost").
EXPO_PUBLIC_API_URL=http://SEU_IP_LOCAL:3333
